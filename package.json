{
  "name": "cleanops-windows-setup",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker for the CleanOps Windows setup script",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}

